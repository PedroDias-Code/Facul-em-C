//Pedro Henrique Dia Soma | Soma 
#include <stdio.h>

int main() {
    int v[5] = {10, 30, 50, 70, 90}, soma;
    soma = v[0] + v[2];

    printf("A soma do primeiro com o terceiro elemento e: %d\n", soma);

    return 0;
}
